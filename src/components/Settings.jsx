const Settings = (props) => {
  return (
    <div className="settings">
        <h1>Settings</h1>
        <div className="settingsOption">
            <div>Dark Theme</div>
        </div>
    </div>
  );
};

export default Settings;
